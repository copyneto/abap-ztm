@EndUserText.label: 'CDS de Autorização'
@MappingRole: true
define role ZC_TM_MDF {
    grant
        select
            on
                ZC_TM_MDF
                    where
                        inheriting conditions from entity ZI_TM_MDF;
                        
}